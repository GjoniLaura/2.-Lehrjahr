@page "/"
@using Restaurant_Reservation.Klassen
@using Restaurant_Reservation.Service
@using Restaurant_Reservation.Datenbank
@inject NavigationManager NavigationManager

<PageTitle>Restaurants</PageTitle>

<div class="container">
    <div class="row">
        <div class="col-md-10 offset-md-1">
            <h3 class="text-center mb-4">Willkommen auf unserer Restaurantreservierungsseite!</h3>
            <p class="text-center">
                <i>
                    Entdecken Sie neue kulinarische Erlebnisse und reservieren Sie Ihren Tisch direkt online!<br />
                    Wählen Sie Ihr Lieblingsrestaurant, erfahren Sie mehr über sein Angebot und die Öffnungszeiten,<br />
                    und sichern Sie sich Ihren Platz für ein unvergessliches Essen.
                </i>
            </p>

            @if (filteredRestaurants.Any())
            {
                <div class="row">
                    @foreach (var restaurant in filteredRestaurants)
                    {
                        <div class="col-lg-4 col-md-6 mb-4">
                            <div class="restaurant-block">
                                <h3 @onclick="() => NavigateToReservationPage(restaurant)" style="cursor: pointer;">@restaurant.Name</h3>
                                <p>
                                    <strong>Standort:</strong><br />
                                    @restaurant.Location.Street @restaurant.Location.HouseNumber,<br />
                                    @restaurant.Location.City, @restaurant.Location.PostalCode, @restaurant.Location.Country
                                </p>
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <p class="text-center">Keine Restaurants gefunden.</p>
            }
        </div>
    </div>
</div>

@code {
    private List<Restaurant> restaurants = new List<Restaurant>();
    private List<Restaurant> filteredRestaurants = new List<Restaurant>();
    private string searchTerm = "";

    protected override async Task OnInitializedAsync()
    {
        try
        {
            restaurants = await DatabaseHelper.GetAllRestaurants();
            filteredRestaurants = new List<Restaurant>(restaurants); // Initialize filteredRestaurants
        }
        catch (Exception ex)
        {
            // Log the exception
            Console.WriteLine($"Error fetching restaurants: {ex.Message}");
        }

        FilterRestaurants();
    }

    private void FilterRestaurants()
    {
        if (string.IsNullOrWhiteSpace(searchTerm))
        {
            filteredRestaurants = new List<Restaurant>(restaurants);
        }
        else
        {
            filteredRestaurants = restaurants.Where(r => r.Name.Contains(searchTerm, StringComparison.OrdinalIgnoreCase)).ToList();
        }
    }

    private void NavigateToReservationPage(Restaurant restaurant)
    {
        NavigationManager.NavigateTo($"/reservation/{restaurant.Id}");
    }
}

<style>
    body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        font-size: 16px;
        color: #333;
        margin: 0;
        padding: 0;
    }

    h1, h2, h3, h4, h5 {
        font-weight: bold;
        color: #333;
    }

    h3 {
        font-size: 2rem;
        margin-bottom: 20px;
        text-align: center;
    }

    .container {
        margin-top: 50px;
    }

    .restaurant-block {
        background-color: #f9f9f9;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        padding: 20px;
        transition: transform 0.3s ease-in-out;
        height: 100%;
    }

        .restaurant-block:hover {
            transform: translateY(-5px);
        }

        .restaurant-block h3 {
            margin-top: 0;
            font-size: 1.5rem;
            color: #333;
            cursor: pointer;
        }

        .restaurant-block p {
            margin-bottom: 0;
            color: #666;
        }
</style>
